import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "MetricUI vs Tremor — Detailed Comparison",
  description: "Compare MetricUI and Tremor for React dashboards. Dashboard wrapper, cross-filtering, drill-down, AI insights, and 18 chart types vs Tremor's component library.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
