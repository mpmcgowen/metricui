import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "MetricUI vs shadcn/ui Charts — Detailed Comparison",
  description: "Compare MetricUI and shadcn/ui Charts. Own-the-code vs ship-the-dashboard. See the lines-of-code difference, chart types, accessibility, and dashboard features.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
