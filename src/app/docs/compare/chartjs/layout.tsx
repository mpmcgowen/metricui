import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "MetricUI vs Chart.js — Detailed Comparison",
  description: "Compare MetricUI and Chart.js for React data visualization. SVG vs canvas rendering, accessibility, CSS theming, React integration, and dashboard features.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
